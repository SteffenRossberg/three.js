server {
    listen 443 ssl;
    server_name ${EDITOR_DOMAIN};

    ssl_certificate /etc/letsencrypt/live/${EDITOR_DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${EDITOR_DOMAIN}/privkey.pem;

    location / {
        root /app;
        index editor/index.html;
    }
}
